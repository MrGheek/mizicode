<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!--
      Hardened Runtime entitlements for Mizi.
      Required for Apple notarization. Only enable what the app actually needs;
      extra entitlements widen the security surface and can trigger App Review
      scrutiny if you ever submit to the Mac App Store.
    -->

    <!-- Allow JIT compilation — Electron's V8 engine requires this. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!--
      Allow unsigned executable memory — needed by Electron's renderer process
      for WASM and some Node.js native addons.
    -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!--
      Disable library validation so that Electron's bundled Node.js native
      modules (better-sqlite3, etc.) can load without being Apple-signed.
    -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Network access — needed to reach the local API server. -->
    <key>com.apple.security.network.client</key>
    <true/>

    <key>com.apple.security.network.server</key>
    <true/>
  </dict>
</plist>
