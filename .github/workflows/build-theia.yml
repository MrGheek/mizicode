name: Build MIZI Theia

on:
  push:
    branches: [main]
    paths:
      - "docker/mizi-theia/**"
  pull_request:
    paths:
      - "docker/mizi-theia/**"
  workflow_dispatch:

concurrency:
  group: mizi-theia-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build:
    runs-on: ubuntu-22.04-16core
    permissions:
      contents: read
      attestations: write
      id-token: write

    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install
        working-directory: docker/mizi-theia

      - name: Compile MIZI extensions
        working-directory: docker/mizi-theia/packages/mizi-extensions
        run: npx tsc

      - name: Build Theia
        run: npx theia build
        working-directory: docker/mizi-theia

      - name: Package build artifact
        run: |
          tar czf /tmp/mizi-theia.tar.gz \
            --exclude=node_modules/.cache \
            -C docker/mizi-theia \
            src-gen/ lib/ node_modules/ package.json packages/mizi-extensions/lib packages/mizi-extensions/package.json

      - name: Upload artifact
        uses: actions/upload-artifact@65c4c4a1ddee5b72f698fdd6a78d2f696daf8135 # v4.6.0
        with:
          name: mizi-theia-${{ github.sha }}
          path: /tmp/mizi-theia.tar.gz
          retention-days: 90

  publish:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
      attestations: write
      id-token: write

    steps:
      - uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d16 # v4.1.8
        with:
          name: mizi-theia-${{ github.sha }}
          path: /tmp

      - name: Create or update GitHub release
        uses: softprops/action-gh-release@c95fe605939608b0b5b16e66aec4c439d7e2fa00 # v2.2.1
        with:
          tag_name: theia
          name: MIZI Theia Artifact
          body: "Auto-built Theia IDE artifact from commit ${{ github.sha }}"
          files: /tmp/mizi-theia.tar.gz
          fail_on_unmatched_files: false
          make_latest: true
          prerelease: false
