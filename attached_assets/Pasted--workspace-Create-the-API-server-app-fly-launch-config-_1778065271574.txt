~/workspace$ # Create the API server app
fly launch \
  --config artifacts/api-server/fly.toml \
  --dockerfile artifacts/api-server/Dockerfile \
  --name mizi-api \
  --no-deploy

# Create the persistent volume for memory storage
fly volumes create mizi_memory --app mizi-api --region ord --size 1

# Set required secrets on the API app
fly secrets set --app mizi-api \
  VASTAI_API_KEY="<your key>" \
  AI_INTEGRATIONS_OPENAI_API_KEY="<your key>" \
  AI_INTEGRATIONS_OPENAI_BASE_URL="https://api.openai.com/v1"

# Create the dashboard app
fly launch \
  --config artifacts/dashboard/fly.toml \
  --dockerfile artifacts/dashboard/Dockerfile \
  --name mizi-dashboard \
  --no-deploy
fly: command not installed. Multiple versions of this command were found in Nix.
Select one to run (or press Ctrl-C to cancel):
fly     7.13.0   Command line interface to Concourse CI
Adding fly to .replit
error: Unknown command `launch'. Please specify one command of: abort-build, active-users, archive-pipeline, builds, check-resource, check-resource-type, checklist, clear-resource-cache, clear-task-cache, clear-versions, completion, containers, curl, delete-target, destroy-pipeline, destroy-team, disable-resource-version, edit-target, enable-resource-version, execute, expose-pipeline, format-pipeline, get-pipeline, get-team, help, hide-pipeline, hijack, jobs, land-worker, login, logout, order-instanced-pipelines, order-pipelines, pause-job, pause-pipeline, paused-jobs, paused-pipelines, pin-resource, pipelines, prune-worker, rename-pipeline, rename-team, rerun-build, resource-versions, resources, schedule-job, set-pipeline, set-team, status, sync, targets, teams, trigger-job, unpause-job, unpause-pipeline, unpin-resource, userinfo, validate-pipeline, volumes, watch or workers
error: unknown flag `app' to update.
error: Unknown command `secrets'. Please specify one command of: abort-build, active-users, archive-pipeline, builds, check-resource, check-resource-type, checklist, clear-resource-cache, clear-task-cache, clear-versions, completion, containers, curl, delete-target, destroy-pipeline, destroy-team, disable-resource-version, edit-target, enable-resource-version, execute, expose-pipeline, format-pipeline, get-pipeline, get-team, help, hide-pipeline, hijack, jobs, land-worker, login, logout, order-instanced-pipelines, order-pipelines, pause-job, pause-pipeline, paused-jobs, paused-pipelines, pin-resource, pipelines, prune-worker, rename-pipeline, rename-team, rerun-build, resource-versions, resources, schedule-job, set-pipeline, set-team, status, sync, targets, teams, trigger-job, unpause-job, unpause-pipeline, unpin-resource, userinfo, validate-pipeline, volumes, watch or workers
error: Unknown command `launch'. Please specify one command of: abort-build, active-users, archive-pipeline, builds, check-resource, check-resource-type, checklist, clear-resource-cache, clear-task-cache, clear-versions, completion, containers, curl, delete-target, destroy-pipeline, destroy-team, disable-resource-version, edit-target, enable-resource-version, execute, expose-pipeline, format-pipeline, get-pipeline, get-team, help, hide-pipeline, hijack, jobs, land-worker, login, logout, order-instanced-pipelines, order-pipelines, pause-job, pause-pipeline, paused-jobs, paused-pipelines, pin-resource, pipelines, prune-worker, rename-pipeline, rename-team, rerun-build, resource-versions, resources, schedule-job, set-pipeline, set-team, status, sync, targets, teams, trigger-job, unpause-job, unpause-pipeline, unpin-resource, userinfo, validate-pipeline, volumes, watch or workers
~/workspace$ 