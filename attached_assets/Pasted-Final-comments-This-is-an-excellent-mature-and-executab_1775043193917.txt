Final comments: This is an excellent, mature, and executable plan. You've refined it well — the GPU Profile System adds real user value, the performance estimates and disclaimers are realistic, the pre-built Docker + parameterized on-start approach is smart, and the prioritization (starting with manual validation) is spot-on. This now feels like a complete blueprint for a personal "Replit with powerful self-hosted Kimi 2.5 + claw-code" that you can actually build and use.
What's Particularly Strong

GPU Profiles — Great addition. Abstracting complexity (search filters, quants, llama.cpp args, expected speed/cost) into selectable tiers makes the dashboard feel professional and approachable. Showing estimates upfront + disclaimer is perfect UX.
Quant sizes & model strategy — Accurate now (UD-TQ1_0 ~245 GB, Q3_K_M ~490 GB, IQ4_XS ~547 GB). The volume + resume logic, plus profile-specific defaults, effectively solves the startup-time problem.
Pre-built Docker images + parameterized script — This is the right engineering decision. One base script + env vars keeps things maintainable. Pre-installing common dev tools (git, React/Vite/Next.js starters, Python packages) will make the environment feel instantly productive.
Real-time status, cost tracking, iframe embed, fallback to code-server — All thoughtful touches that will make the experience feel polished.
Tasks list — Well-ordered. Manual validation first is crucial and will catch most gotchas early.
Nice-to-haves in Task 10 — Practical (logs viewer, upgrade quant button, dynamic nginx proxy auto-detection).

Very Minor Final Polish Suggestions

Quant Accuracy (Tiny Tweaks)
Starter/Standard: UD-TQ1_0 is consistently listed around 240–245 GB — your numbers are fine.
Pro: Q3_K_M ≈ 489–490 GB.
Ultra: IQ4_XS ≈ 547 GB.
You can keep the table as-is; it's close enough for estimates.

Performance Numbers
Your ranges are realistic and conservative (good). On 4x 4090 with UD-TQ1_0 and proper offloading, 10–22 tok/s is achievable for most coding/agent tasks. Ultra numbers look plausible with heavy hardware.
The disclaimer you added is perfect.

Vast.ai Template / On-Start Details
Vast strongly encourages using PROVISIONING_SCRIPT (a remote URL or inline script) on top of recommended base images rather than fully custom Docker images for everything. Your hybrid approach (pre-built images + parameterized on-start) works well, but during validation, test both ways. Hosting the on-start script at a stable GitHub raw URL is clean and easy to update.
For nginx dynamic preview: Auto-detecting the running dev server port is ambitious but valuable — start simple (manual port or fixed /preview proxy) and enhance later.

Bolt.diy Integration
Bolt.diy supports custom OpenAI-compatible endpoints well (via Settings > Models > OpenAI-like server or env vars). Pointing it to http://localhost:8081/v1 (no key needed) should work for chat/agent mode. Test tool calling early in the manual validation step.

Small Enhancements to Consider
In the dashboard profile cards, add "First launch: ~15-20 min | With volume: ~2-5 min" — users hate surprises on wait time.
Session history with total_cost calculation is nice; even a simple running timer * rate is useful for single-user.
Basic auth/token on exposed services remains important even for personal use.


Final Verdict
This plan is ready to go. It's thoughtful, pragmatic, and balances ambition with feasibility. The core value proposition — spin up a beautiful Bolt.diy environment with real Kimi 2.5 + claw-code on cheap/rented GPUs, on-demand, with clean previews — is very strong.
Immediate next step (Task 1) remains the most important: manually validate the core loop on a real 4x4090 instance. Once you confirm that llama.cpp serves Kimi, Bolt.diy connects and works (including agent/tool use), previews function via nginx, and code-server is accessible, the rest of the project becomes much lower risk.
If everything validates cleanly, building the pre-built Docker image and parameterized on-start script will feel straightforward.
You've iterated this plan thoughtfully — it shows good understanding of the practical challenges (model size, startup time, Vast API quirks, UX). This should deliver the smooth, powerful coding experience you've been aiming for.