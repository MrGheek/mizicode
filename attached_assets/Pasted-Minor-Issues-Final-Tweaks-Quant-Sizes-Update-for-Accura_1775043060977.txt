Minor Issues & Final Tweaks

Quant Sizes (Update for Accuracy)
UD-TQ1_0 ≈ 240–245 GB → Good for Starter.
Q3_K_M ≈ 489–490 GB → Fine for Standard/Pro.
IQ4_XS ≈ 547 GB → Correct for Ultra.
Your table numbers are close enough, but you can use the exact figures above in the dashboard for transparency.

Performance Estimates (Slightly Conservative is Better)
Starter (1x 4090 + UD-TQ1_0 with heavy offload): Realistic ~5–12 tok/s (not 5–8).
Standard (4x 4090): ~10–22 tok/s is more typical with good offloading.
Pro/Ultra numbers look reasonable.
Add a note in the dashboard: “Actual speed depends on context length, quant, and offloading efficiency. First startup includes model download time.”

Docker Image Strategy
Great use of build args and multi-stage builds.
Pre-clone Bolt.diy + claw-code inside the images (as you noted) will shave several minutes off startup — excellent.
Consider also pre-installing common dev tools (git, node packages for React/Vite/Next.js, Python deps, etc.) to make the environment feel instantly productive.

nginx Preview Proxy
Dynamic proxy on port 3000 is good. A common pattern is to proxy /preview → the detected running dev server port (or let users specify the port in Bolt.diy settings). Test this early.

Vast.ai Template / API Notes
Templates support onstart as a shell command string or a script URL. Hosting the parameterized script at a stable GitHub raw URL is clean and easy to update.
When creating instances, you’ll use the correct template_hash_id + profile-specific env overrides (MODEL_QUANT, LLAMA_EXTRA_ARGS, etc.). Your API layer plan already covers this.

Bolt.diy Integration
Bolt.diy has solid support for custom OpenAI-compatible endpoints (via Settings → Local Providers or env vars). Pointing it to http://localhost:8081/v1 should work well. Test agent/tool-calling mode early during manual validation.


Quick Wins / Nice-to-Haves for v1

Show estimated time to ready per profile (e.g., “~15–20 min first launch, ~3 min with pre-loaded volume”).
Add a simple “Upgrade quant” button later that triggers a volume-mounted heavier model.
Include basic logging dashboard link (view /var/log/onstart.log via code-server or a simple web viewer).